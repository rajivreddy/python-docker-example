apiVersion: v1
kind: Service
metadata:
  name: {{ include "python-flask-example.fullname" . }}
  labels:
{{ include "python-flask-example.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetport }}
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "python-flask-example.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
